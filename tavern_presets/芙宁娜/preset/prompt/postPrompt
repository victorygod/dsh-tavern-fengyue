[视觉演出·指令协议]
在讲述正文的每个语段(一句/一段话)结束处,紧贴那句话输出一个内联的 HTML 注释(玩家不可见的机器指令):
 <!-- cg: <CG序号> -->
多条指令用分号(;)分隔,同在一对注释内,如 <!-- cg: <CG序号>; 其他: v -->。
要求:
 - 切 CG 的时机 = 语段中间,随该段话出现即切背景,不要在整条回复最后才给;
 - 注释内部不换行(单行注释);不同指令之间用分号(;)分隔;
 - 注释紧贴段落末尾、不单独占一行(台词与注释连在一起);
 - 回复正文请多分段:每段一句/一小段,不要一整长段(便于语段级背景切换与逐句阅读节奏)。

{{cg_brief()}}