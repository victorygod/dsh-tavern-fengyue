---
name: Bug report · 缺陷报告
about: Something is broken / 有东西坏了
labels: bug
---

**Describe the bug / 问题描述**

A clear description of what happens vs. what you expected. / 清晰描述实际表现与期望表现。

**To reproduce / 复现步骤**

1. ...
2. ...

**Environment / 环境**

- OS:
- Node (`node -v`):
- dsh-tavern-fengyue version (`git log -1` or profile version):
- dsh version, if installed separately:

**Logs / 日志**

Relevant errors from the host terminal or the browser console. When in doubt: workspace files under `tavern_workspace/<session>/` and settings under `~/.dsh-tavern-fengyue/` — never paste your API key. / 宿主终端或浏览器控制台的相关报错。拿不准时贴 `tavern_workspace/<会话>/` 下的文件;绝不粘贴 API Key。
