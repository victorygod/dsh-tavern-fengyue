/**
 * Source-only dev/testkit package: not built by the workspace faces; tests
 * resolve its sources directly. An empty entry removes this package from the
 * tsdown workspace build (preset-side convention).
 */
export default { entry: '' }
